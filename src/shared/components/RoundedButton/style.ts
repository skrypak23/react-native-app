import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  button: {
    width: 60,
    height: 60,
    position: 'absolute',
    bottom: 30,
    right: 30
  },
  icon: {marginLeft: 21}
});

export default styles;