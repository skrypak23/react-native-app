import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  form: {
    marginBottom: 40
  },
  button: {
    fontSize: 16,
    color: '#fff'
  },
  badgeWrapper: {
    flex: 1,
    flexDirection: 'row',
    justifyContent: 'center',
    marginBottom: 10
  },
  badge: { marginLeft: 'auto', marginRight: 'auto' },
  badgeText: { color: 'white', padding: 5 }
});

export default style;
