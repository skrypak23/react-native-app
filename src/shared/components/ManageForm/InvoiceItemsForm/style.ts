import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  form: {
    marginBottom: 40
  },
  button: {
    fontSize: 16,
    color: '#fff'
  },
  cardItem: {
    height: 70
  }
});

export default style;
